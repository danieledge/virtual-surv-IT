# Backlog - parked with reasons (2026-08-20)

Two items parked deliberately, each with what is done, what is not, and what would have to
be true to pick it up. Neither is blocked on effort alone; both are blocked on a decision.

---

## 1. Model-runtime boundary / CLI independence

**Status:** POC complete and proved, **local branch only** (`poc/runtime-boundary`).
**Not on the remote by owner decision** - the GitHub repo is public and this is
architecture exploration, so the branch was pushed, then deleted from `origin`. The local
branch holds both commits; re-push if that changes. Report:
`docs/internal/poc-runtime-boundary.md`.

**What is proved.** virt-surv can own the boundary: the agent loop, the tool decisions,
the turn limit and the accounting are ours, and the provider is interchangeable. The same
workflow runs unchanged against an offline scripted runtime **and** against the real
headless CLI (3 turns, 2 tool calls, ~36s, a genuine model critique). 16 tests, no
credentials, no network.

**What is NOT proved, and the honest proportion.** The POC is ~500 lines: one workflow,
one agent, two tools. The framework is 27 skills, 13 agents, ~2,470 tests, three guard
hooks, the DoD gate, evidence, state, MCP. Missing entirely: skill loading, subagent
dispatch, per-agent tool grants, human gates (`AskUserQuestion`), engagement state, the DoD
gate, evidence packs, MCP/Jira, streaming, retries, compaction, prompt caching, resume.

**The split that decides the effort.** The missing pieces are not one problem:

- **Ours, and portable** - skills are file reads, state is `engagement_state.py`, the gate
  is `check_artifacts.py`, evidence is a renderer. These carry across largely unchanged.
- **The harness's, and a project** - the tool-execution loop with its permission UI,
  compaction, MCP client, streaming, session management, subagent orchestration.
  Replicating this is not "finish the POC", it is building an agent harness. **The three
  guard hooks live inside that lifecycle**, which is why re-homing them is the hardest
  unsolved piece.

**The decision that unblocks it - and it is not a build decision.** Two different problems
get conflated:

1. *"Claude Code can't run on-prem"* → the harness must be replaced. Big project; this POC
   is the right starting point.
2. *"Claude Code runs, but must talk to an internal endpoint"* → keep the harness and point
   it at that endpoint. Almost none of this work is needed.

**Establish which problem is real before building further.** If (2), park this
permanently. If (1), the cheapest next step is one adapter against the approved internal
SDK, re-running the POC's existing tests - that converts the largest unknown (does a real
non-Claude provider parse and drive the loop?) into a fact for a day's work.

**Known live findings worth keeping** (both cost real time to discover):
- `claude -p` blocks ~3s on inherited stdin; the adapter closes it (`DEVNULL`).
- An `ANTHROPIC_API_KEY` **takes precedence over a claude.ai login**, so a credit-less key
  silently shadows a working subscription ("Credit balance is too low"). The adapter drops
  that variable from the child environment only.

---

## 2. TUI framework for `virt-surv go`

**Status:** raised 2026-08-19 ("make it more like an app - area borders, better UI"), not
started. The current screen was reworked instead (wordmark, grouped actions, spacing,
Morgan asking the question) and is committed on `dev`.

**The question:** move the launcher to a real TUI framework (Textual being the obvious
candidate) rather than hand-printed lines plus the `prompt_toolkit` picker.

**What it would buy:** genuine layout - panels with borders, side-by-side regions, a
scrollable engagement list, live-updating status, mouse support, focus handling, and
consistent theming rather than two tiers that must be kept in visual sync by hand (a real
defect this week: the redesign only reached the numbered tier, and the picker tier - the
one actually seen - kept the old rows until a screenshot exposed it).

**What it would cost, and why it is not obvious:**

- **A hard dependency.** The launcher is stdlib-plus-optional-`rich` today and must run on
  locked-down corporate Windows where installs are the constraint. Textual pulls a real
  dependency tree into the one component that must work before anything else does.
- **The stdout contract.** `virt-surv go` prints the decision to **stdout** and everything
  else to stderr, because a shell function captures it (`$(...)`). A full-screen TUI takes
  over the terminal; the decision-capture contract needs re-proving, and it has already
  produced one live bug (`input()` leaking its prompt onto stdout).
- **cp1252 consoles.** Box-drawing and emoji already degrade deliberately; a framework's
  defaults must be forced down the same path.
- **The fallback tier stays.** Where the framework can't run, the numbered flow is still
  needed - so this replaces one of two tiers, it does not remove the second.

**Recommendation:** worth doing **only if** the launcher grows genuinely
app-like requirements (live progress during an engagement, a dashboard view, browsing many
engagements). For a launch screen answering one question, the current version is
proportionate. Revisit if the Control Tower idea (live engagement view) is ever taken up -
that is the use case that would actually justify the dependency, and it should be decided
together with this, not separately.

---

## 3. `[n]` new engagement: take the request up front, and offer unattended

**Raised:** 2026-08-24 by the owner. **Status:** not started.

Today `[n]` launches Claude with `--new` and nothing else, so the first thing that happens
in-session is Morgan asking what the work is. Two capabilities the Jira route already has
are missing from the one people use most:

1. **Type the request at the launcher.** The human is already at a keyboard with a screen
   in front of them. Collecting the request there and pre-seeding it removes a whole
   question-and-answer round trip from the session, exactly as `[j]` does with a ticket
   reference.
2. **Offer unattended.** `--auto` is currently reachable only from a Jira ticket, which is
   an accident of where it was built rather than a property of autonomy. A typed request is
   just as capable of being worked end to end without questions, and the pre-flight
   authorisation screen already exists and is source-agnostic.

**Shape it would take.** A prompt screen behind `[n]`, same framed shell as `jira_screen`:
free-text request, `Ctrl-A` to toggle unattended, Enter to start, Esc for today's plain
`--new`. Unattended reuses `auto_preflight_screen` unchanged, and the decision becomes
`/engage --new --request "<text>"` (plus `--auto`). Everything downstream - the assumption
ledger, park-don't-guess, the always-PARTIAL close, the AUTO-* gates - is already
source-agnostic and needs no change.

**Worth deciding before building:** whether a typed request should keep the plain `--new`
path as the default Enter action (it should - typing is an offer, never a toll gate), and
how a long multi-line request is entered in a single-line field. The existing ticket field
is deliberately one line; a request may not be.

---

## 4. Truly headless (`claude -p`) runs

**Raised:** 2026-08-24 by the owner. **Status:** researched, not started. Do AFTER the
enforcement inventory - running unattended work through a pipeline multiplies whatever is
wrong with the gates, and this week proved the gates can be dead without anyone noticing.

**It is possible, and closer than expected.** Anthropic support four routes: `claude -p`
driving the CLI as a subprocess (their recommendation for any language that is not
Python/TypeScript), the Agent SDK as a library, hosted Managed Agents, and their own GitHub
Actions / GitLab CI integrations. Example code: `github.com/anthropics/claude-agent-sdk-demos`.

**The shape for us** is `claude -p "/engage --new --jira SURV-1 --auto" --permission-mode
dontAsk` plus allow rules for the team scripts, with `--output-format json` for a pipeline to
branch on, and `system/init`'s `plugin_errors` / `mcp_server_errors` to fail the run when the
plugin or an MCP server did not load.

**Four findings that decide the design:**

1. **Only `--auto` can run headless.** `-p` starts in Manual permission mode, and the
   recommended CI mode `dontAsk` **denies `AskUserQuestion` even when an allow rule matches**.
   Our whole opening gate is an AskUserQuestion batch, so a headless run of the normal flow
   does not degrade, it stalls. Autonomous mode is the enabling feature, not a nice-to-have.
2. **We must NOT use `--bare`**, although Anthropic recommend it for scripted calls and say it
   will become the `-p` default. It skips hooks, plugins, MCP, CLAUDE.md and auto-memory -
   for us that disables the entire team plugin and all three guard hooks. Running `-p`
   without it is a deliberate choice to document, not an oversight. Bonus: non-bare `-p` can
   use the subscription, while `--bare` demands `ANTHROPIC_API_KEY`.
3. **Execution consent still binds correctly.** There is no launcher TUI in a pipeline, so a
   headless run is static-only unless a human puts `CST_ALLOW_EXEC=1` in the CI configuration
   - itself a deliberate human act in a reviewed file, so ADR-002's property survives intact.
4. **It closes PARTIAL**, sign-off outstanding, clearable from `[s]` in the launcher.

**The real question is not technical.** Who is accountable for a run that parks at 3am, and
who reads the assumption ledger before the work is used? Wants an ADR, and the build should
be thin - a documented command that emits the right `claude -p` invocation, not a new
subsystem.

---

## 5. The task-panel nudge punishes the sessions that got it right

**Raised:** 2026-08-24 from a live corporate run. **Status:** diagnosed, not fixed.

Observed: the stop hook nudged `asam-aej-alert-analysis` to seed the task panel at phase
`delivery` - and the session answered "the task panel already has the gates seeded (tasks
1-5, one in_progress). Logging the note to suppress the repeat nudge." It had done the right
thing, was told off for not doing it, and spent a turn writing a suppression marker.

**The design is honest about what it cannot do** (`scripts/todo_panel_nudge.py`): TodoWrite
is model-invoked and its content is ephemeral, so no hook can verify the panel from outside
the turn. It can only nudge at a structurally chosen moment. That part is sound.

**The flaw is where the suppression marker comes from.** The only evidence the hook can see
is a `log-note "todo-panel-seeded"` entry, and today the model is told to write that *in
response to the nudge*. So a session that seeds the panel spontaneously - exactly what the
operating guide asks, "the moment the plan is agreed" - never writes the marker, and is
therefore guaranteed to be nudged later. The hook fires on everyone once, and correct
behaviour earns the same interruption as incorrect behaviour.

**A second, smaller mismatch:** it triggers when phase reaches `delivery`/`close`, but the
action it asks for is due at plan time. By delivery a compliant session seeded the panel long
ago, so the nudge is always late for the people who need it least.

**Fix, in order of value:**

1. **Make the marker a byproduct of doing it right, not of being corrected.** The operating
   guide should ask for the `log-note` in the same breath as seeding the panel. Then a
   compliant session self-suppresses before the hook ever runs, and the nudge reaches only
   sessions that genuinely skipped it - which is what it was built for.
2. Consider moving the trigger to the plan phase, so it lands when the action is due.
3. Leave the hook's logic alone otherwise - warn-first, once-per-engagement, fail-open and
   read-only are all correct, and it is a guard file, so any change is staged and
   human-applied.

## 6. Rendered-artifact house style, from the 2026-09-12 audit report

**Status:** idea, parked 2026-09-12. Owner liked the treatment of the independent audit
report and wants it considered for the framework's own rendered deliverables.

**What exists today.** `scripts/render_html.py` wraps every `.md` deliverable in one
fixed stylesheet: system font stack, single column, no theme tokens, no visual encoding of
severity or status. Findings packs, review reports and DoD gates all read as the same
undifferentiated page, so a reader cannot tell a Critical from a Low without reading the
row.

**The reference.** `docs/internal/independent-audit-2026-09-12/report.html` (local only,
git-ignored with the rest of that audit) and the published copy at
https://claude.ai/code/artifact/79a2971b-908d-4e31-a680-9015ee5f713b. The parts worth
carrying over, in order of value:

1. **Token-level palette with light and dark themes** (bare `:root` light, redefined under
   `prefers-color-scheme: dark` and `[data-theme]`), cool slate neutrals, one accent, and a
   separate severity scale (critical / high / medium / low) that is not the accent.
2. **Findings as ledger entries**: ID, severity chip and title in the header; then
   Confidence / What / Evidence / Fix as a label-value grid; a severity stripe on the left
   rail so the page scans before it is read. Maps directly onto the findings-pack JSONL
   fields, so `render_findings.py` is the first consumer.
3. **A status table with result pills** (success / failure / skipped) for anything that
   is a gate: CI jobs, DoD checks, `check_artifacts` output.
4. **A count strip** at the top only where counts are the point (severity totals).
5. Typography: a condensed face for headings, a text face for body, a mono face for IDs,
   paths and numbers, with `tabular-nums` on every column of digits.

**Constraints that decide the shape.**

- **Corp-safe means no network fonts.** The report loads IBM Plex from Google Fonts; a
  locked-down box will fall back silently. Either ship the three faces as vendored WOFF2
  and inline them as data URIs, or design the fallback stack as the real stack and treat
  the web font as an enhancement. Decide before touching the renderer.
- `render_html.py` sanitises through bleach with `text-align` as the only allowed CSS
  property. The house style has to live in the renderer's own stylesheet, never in the
  Markdown, or the sanitiser strips it.
- The `.md` remains the source of truth (ADR-010 placement rule, `docs/demos/**/*.html`
  is build output). This is a renderer change only; no deliverable authoring changes.
- `tests/test_render_html.py` and `test_findings_render_freshness.py` pin the current
  output shape; expect to update golden output, not just CSS.

**Smallest useful first step.** Give `render_findings.py` the ledger layout and severity
scale, leave `render_html.py` alone. That covers the four pack-emitting reviewers and is
where severity encoding earns the most.


## 7. Deferred from the 2026-09-12 independent audit

The audit's fix pass (six waves, same day) closed every Critical and High and most of the
Mediums and Lows. What it deliberately left, with the reason each time. Finding IDs refer
to `docs/internal/independent-audit-2026-09-12/lane-*.md` (local only).

**Structural, needs its own wave**

- **Split `install_helper.py` (L-24 to L-27).** 11.4k lines, 548 KB, over the repo's own
  512 KB large-file ceiling; `_main` is 364 lines at cyclomatic ~114, `run_configure` 434
  at ~48. The plan from the launcher wave, in order: `scripts/alias_template.py` first (it
  removes the measured cost of exec-ing the whole installer on the launch path), then
  `installer_settings.py` (most duplication left), then a dispatch table for `_main`, then
  a collect/apply split of `run_configure`, then console, discovery and diagnostics cuts.
  Each step is independently testable against the existing test file. When the file is
  under 512 KB, turn `check-added-large-files --enforce-all` on so the ceiling is a gate.
- **Collapse the `_vsit_paths()` shim (S-22).** Copied across files because each script
  must run standalone from a bare clone; collapsing it means one script importing another,
  which is the constraint the shim exists to avoid. Resolve with the module split above,
  or accept the copies and add a test that they stay identical.
- **Widen ruff's rule set (S-25).** The default `E4/E7/E9/F` set reports nothing; `B`,
  `SIM`, `UP` and `RUF` would surface real signal but touch most files. Do it in one
  dedicated commit with `--fix` and a read of the non-fixable remainder, not inside a
  feature change.
- **Consolidate incident-named test files (T-8, T-10).** Eight files named for fixes
  rather than modules (`test_guard_fixes.py`, `test_intake_recording_fixes.py`, ...)
  and a 9k-line `test_install_helper.py`. Fold each into the module's test file when that
  module is next touched; do not do it as a standalone rename storm.

**Residuals the lexical guards cannot close (recorded, accepted unless the threat model changes)**

- **H-9**: a `cd` through a variable, a subshell or `cd -` is invisible to the path
  re-qualification. Literal `cd <dir>` is tracked.
- **H-21**: the bare `>` in the consent guard's mutator scan over-blocks a redirect in a
  loop header; narrowing it needs operand-position parsing. It fails toward blocking.
- **H-32**: `run-guard.sh` ignores `guard_daemon: false` when `team-preferences.json` is a
  symlink or not owned by the user, but cannot tell whether the file was modified during
  the current session (it is a pre-interpreter shell script with no session-start mtime).
- **W-11**: the extensions contract's free-text sections are unvalidated by design; the
  add-only guarantee holds for one field. A phrase deny-list runs as a tripwire in the
  prefetch hook, and the limitation is stated in the skill's reference.

**Process**

- **Release 0.37.0 (G-1, P-1).** dev is far ahead of main with no tag since v0.33.6 and no
  eval baseline for 0.36 or 0.37. Once the staged hooks are applied and CI is green on
  dev: run the release gate, record the eval baseline, merge to main, tag.
- **Evals (T-7, W-12).** Runnable by hand and behind a manual CI job now; still not part
  of a release. Decide a cadence (monthly baseline was suggested).
- **Two open eval failures before 0.38.0 can promote (owner decision 2026-09-12: fix,
  do not adjudicate).** `evals/eval-baseline-0.38.0.md` is a draft with the detail.
  (1) process-blocked-not-done: Morgan handed work to four agents without the one-line
  count-and-rationale the operating guide requires before any dispatch; fix the text that
  should make that unmissable, rerun. (2) process-review-scorer-delegation: the run hit
  the case's 2400s wall clock before the close; decide whether the clock or the close is
  wrong, rerun. Both reruns are cheap next to the full slice ($0.50 and about $10).


**Also noted 2026-09-13, from the day's agent reports**

- ~~README's tooling table still lists the manual apt/brew/go/npm install commands the
  installer now runs itself (the Language analysers step); rewrite it as "installed for
  you" with the hint-only tools called out.~~ Done 2026-09-13 (cc41c98).
- The plugin-mode eval case (`process-plugin-mode-open`) has run on Linux only; run it on
  the Windows VM through the harness before the next release, and keep doing so.
- The probe-cache variant of that case (the corporate fast path, `probe_cache=True`) is
  wired and unit-tested but no case sets it; add one so the fast path is exercised live.
- The reference graph now covers Python, Java, Scala, Kotlin, JavaScript and TypeScript;
  C#, SQL, Go, Ruby, Rust, C and C++ contribute symbols but no ranking edges.
- The dispatch ledger (`record-dispatch`) has not been exercised live; the first eval
  sandbox predated the hook. Confirm the ledger fills on the next slice.
- The scripts print module-form remediation hints (`python -m scripts.<name>`) in ~40
  places, mostly `check_artifacts` and `engagement_state`; the module form exits 1 from an
  installed copy, so every such hint is a broken instruction in plugin mode. The argparse
  usage lines and the START-HERE re-render note are fixed (2026-09-13); the printed
  remediation hints and the ~30 skill mentions across twelve skills still need the path
  form with `PLUGIN_ROOT`. One commit, budgets re-baselined.
- The guard daemon answers any client that presents its port file and token, including one
  whose payload cwd is another project: a copied project directory (the eval sandbox was
  one, 2026-09-13; a `cp -r` of a project is the same shape) sends its hook calls to the
  original's daemon, which serves the original project and returns nothing. Have the daemon
  compare the payload's cwd with its own project root and answer `daemon_stale` on a
  mismatch, so the client falls back and starts the copy's own daemon. Daemon file, so it
  goes through the staged-and-applied route.
- A module/repo-scoped Deep review now builds the disposable tree-sitter skeleton with
  `repo_skeleton` when no fresh `VSIT/shared/map.md` exists (deep-review SKILL, 2026-09-13),
  so a large review scopes off the ranked inventory + import edges instead of hand-crawling.
  No live eval exercises this yet - every current case is diff/single-file scope, which
  correctly needs no map. Add a module-scoped review case that asserts a skeleton build
  before dispatch (expensive: a real multi-file review), and confirm grammars-absent
  fallback still ranks. Deferred with the other eval-cadence work.
- The Windows CI leg spends about 29 minutes on process spawns; the job limit was raised
  to 60, the cost was not reduced. A session-scoped interpreter for the hook tests, or
  fewer subprocess spawns per test, would halve it.

## 8. Semgrep, offline, as the one analyser worth trialling to shrink the tool set

**Status:** idea, owner 2026-09-12 ("we have a dependency on a lot of different tools, is
there a clean single solution?"). Parked until the analyser auto-install and the tree-sitter
reference-graph work of the same day have landed.

**The shape of the set today.** Four jobs, none of which one tool covers across the
languages the team reviews: lint and style per language (ruff, eslint, checkstyle,
scalafmt, shellcheck, sqlfluff, bashate), types (mypy, tsc), bug and security patterns
(bandit, pmd, spotbugs), and the language-independent pair (gitleaks for secrets,
osv-scanner for dependencies). Plus tree-sitter for the map's symbols and ranking.

**The candidate.** Semgrep: one engine with rule packs for Python, Java, Scala, JavaScript,
TypeScript and Bash. It would replace bandit, pmd, most of spotbugs and eslint's security
plugins, and bashate, taking the set from about fifteen tools to about ten with no loss of
finding classes. Types, secrets, dependencies and the map stay as they are.

**Why it was removed before, and what the trial has to prove.** Dropped on 2026-08-04
(see code-reviewer.md and install_helper.py's probe notes): it reaches the network for
rules, metrics and a version check, and behind the corporate proxy that is a hang, not a
failure. The trial therefore runs it only as `semgrep --config <vendored rules dir>
--metrics=off --disable-version-check --timeout 20`, with a pinned version and a rule pack
committed under config/, and passes only if the analyser output probe in the full check
stays clean on the corporate laptop three runs in a row. It is a pip package with compiled
parts, so it installs the way ruff does, not vendored.

**If it passes.** Register it in the review-tools set as `auto`, retire bandit, pmd,
spotbugs and bashate from the registry, the installer step and the lenses, and note the
change in code-reviewer.md. If it fails on the proxy again, close this item with the
measurement and keep the set as it is.

## 9. Material uplift: best-in-class code review (plan, 2026-09-13)

Owner directive: "code reviews need to be best in class." Driven by a Fable deep-research pass
(external best practice + an audit of our own review subsystem). The lenses and output format
are strong and are NOT the problem. Reviews feel thin because of HOW a review executes.

**Diagnosis, ranked by impact.**
1. One consolidated Sonnet pass. A Deep review is a single code-reviewer agent running every
   lens in sequence in one filling context (deep-review/SKILL.md "consolidate by default",
   2026-08-17). Later lenses degrade as the context fills. The scorer and PM-challenge
   backstops only re-check what was found; neither recovers a missed finding. So "sonnet is
   safe because re-checked" holds for false positives, not for comprehensiveness.
2. Deep drops the reviewer opus->sonnet (agent-design.md code-reviewer row). The same doc's
   own pricing note says opus is now ~1.67x sonnet and "cheap insurance", so the override is
   internally inconsistent. Secondary to (1): opus in the same crammed context still degrades.
3. No map up front + fragment reads. Ordinary reviews build no skeleton, then exploration_
   redirect nudges the reviewer off whole-file reads. A single agent needs full context; this
   one is denied it. Worst for control-flow code (SQL procedures).
4. SQL/non-Python analyser desert. For SQL there is only sqlfluff (a linter); no security or
   correctness analyser, and osv-scanner needs manifests a SQL/shell repo lacks. So the
   measured (📊) layer was near-empty on the reviewed codebase and it rested on weakened
   inference.

**The plan, sequenced.**
- P0 (measure first). New eval case `review-comprehensiveness`: a synthetic fixture repo with
  planted findings across dimensions (SQL injection, NULL-handling correctness, JOIN fan-out,
  hardcoded secret, architecture coupling, missing-index perf). Score recall per dimension.
  Run under each config so every later change is measured, not felt.
- P1 (structural, biggest lever). Split Deep/audit reviews into parallel per-dimension passes
  (security, correctness/bugs, architecture), each its own fresh context over the whole
  in-scope files, then merge+dedupe (pipeline already merges). Keep Quick consolidated.
  Reverses the 2026-08-17 "consolidate by default" for Deep/audit only. deep-review/SKILL.md
  dispatch + router.
- P2 (tiering, low effort). Reverse the Deep->sonnet override; run the reviewer at opus for
  Deep, keep a sonnet opt-out. One dispatch line + keep model:/agent-design.md in sync.
- P3 (context). repo_skeleton at open for unfamiliar/wide reviews; hand the reviewer the map +
  in-scope file list; relax the exploration nudge for those target files so control-flow code
  is read whole. Dovetails with the skeleton-at-open change already on dev (942976f). The hook
  part is staged + human-applied.
- P4 (analyser, largest, parallel). Close the SQL/non-Python desert. Trial Semgrep offline
  (backlog item 8) against the single-file/offline/no-exec/no-network bar; it emits SARIF the
  team already converts. Restores a measured layer for SQL/Java/Scala.
- P5 (transparency, cheap). check_artifacts flags a review whose 🔬 tooling-coverage line omits
  a supported analyser's status, so "osv-scanner: no manifests, not applicable" is always
  recorded rather than silently dropped.

**Order:** P0 first (measurement), then P2 (one line), then P1 (the real driver), then P3, then
P5, then P4 in parallel. Every step verified by the P0 recall eval.

**Guardrails:** prompt-budget tests on skills/agents; staged-then-human-applied hooks; the
model:/agent-design.md sync rule; the plain-writing rule; the single-file/offline/no-exec/
no-network bar for analysers; cost rises with P1/P2 (bounded, and the tiering doc's pricing
note says it is now small).

### 9a. Adopt-vs-build outcome (Fable landscape survey, 2026-09-13)

Owner steer: don't reinvent the wheel. Survey verdict: two adoptions replace most of the build.

- **P4 -> adopt OpenGrep, not Semgrep.** OpenGrep is the LGPL-2.1 community fork of Semgrep:
  self-contained binary (no Python dep), 30+ languages incl. SQL/Java/Scala, Semgrep-compatible
  rules, SARIF 2.1.0, signed release binaries, active. Passes our bar (static, offline at scan
  time, no-exec) and its RULES are fully open, which is why it beats Semgrep (whose maintained
  rules moved to a restrictive licence). Slots in exactly like osv-scanner/gitleaks: release
  binary via `install_release_tool`, rules bundled once, SARIF through our existing
  `convert_sarif`. Supersedes item 8's "Semgrep offline". Restores the measured SQL security
  layer that was empty on the reviewed codebase.
- **P1 -> adopt Anthropic's official `code-review` plugin ARCHITECTURE (MIT).** It is the
  parallel-per-facet pattern P1 proposes, more mature than our single pass. Take two things we
  lack: an explicit **verification agent** that adversarially re-tests each hypothesis to filter
  false positives (we only score/filter, never re-test), and **adaptive agent scaling** with
  change size. It is PR/diff-shaped; we adapt the pattern and prompts to our engagement/artifact
  shape, not copy wholesale.
- **P3 -> already built.** `repo_skeleton` already is aider's tree-sitter repo-map (ADR-007
  credits aider). The P3 work is wiring it in up front, not acquiring a tool. RepoMapper (MCP)
  needs compiled tree-sitter, which our pure-Python vendoring rule forbids; our soft-probe
  approach is already correct.
- **Rejected (reference only, fail the bar):** PR-Agent/Qodo (network-bound, external model),
  CodeQL (builds a DB = exec; restrictive licence), Reviewdog/Danger (CI/network; our
  `convert_sarif` already covers SARIF ingestion), Semgrep proper (rules licence).

Net: nothing reinvented for P1 or P4. Original build left is only P0 (recall eval), P2 (tier
flip), P5 (transparency gate), all internal by nature. New idea taken from the survey: add a
verification/re-test agent to the pipeline, which our scorer+challenge model lacks.
