# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by `npm run data` (python -m scripts.dashboard --json) - regenerated per run,
# never committed. See scripts/validate_references.py's _RUNTIME_PARTS for the doc-link
# allowlist entry this filename needs (it's referenced in prose, not a real repo file).
data/dashboard-data.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
